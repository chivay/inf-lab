/*
 *
 * Jesli to możliwe, odtwórz oryginalne wartości z tablicy
 * uzyskanej w poprzednim zadaniu
 *
 */
#include <stdio.h>

#define MAX 10000

int tab[MAX];
int tab2[MAX];
int main() {
	int n;
	scanf("%d", &n);

	int i;
	for (i = 0; i < n; i++)
		scanf("%d", tab+i);

	// rekonstruujemy kolejne indeksy
	for (i = 0; i < n; i++)
	{
		int w = 0;

	}
	printf("%d ", n);
	for (i = 0; i < n; i++)
		printf("%d ", tab2[i]);
	printf("\n");

	return 0;
}
